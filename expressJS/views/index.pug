html
    head
        title= "Search"
        style.
            #map {
                height: 500px;
                width: 100%;
            }
    body
        h1= "TwittMap"
        h4= "Please select a word you want to search in tweets"
        form(action="/display" method="post")
            select(name="selection")
                option(value="rich") rich
                option(value="strong") strong
                option(value="trump") trump
                option(value="wall") wall
            input(type="submit" value="Search")
            hr
        div(id="map")

        each script in scripts
            script(type='text/javascript', src=script)
        script(src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCYAXFUzdidNb5J_EnXz9Pa6Jbsedt6FdM&callback=initMap")